using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExampleTestReadPythOrSol : MonoBehaviour
{
    public int summonerIDNumber;
    public int currentSummonerLevel;
    public int pyTestSummonerIDNumber;
    public int pyTestCurrentSummonerLevel;

    //public conftest conftestPythScript;
    //public attributes attributesSolScript;

    // Start is called before the first frame update
    void Start()
    {
        /*
        summonerIDNumber = attributesSolScript.uint;
        currentSummonerLevel = attributesSolScript.level;
        pyTestSummonerIDNumber = conftestPythScript.account;
        pyTestCurrentSummonerLevel = conftestPythScript.rm;
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
